# Compact calibration rubric — human adjudications #1–#85

The extraction contains 85 original human decisions: 21 `APPROVE`, 64 `REVISE/MINOR`, and no `REVISE/SEVERE`. These counts calibrate examples only; they are not a target rate.

Approve low-risk figurative language, rhetorical intensity, ordinary narrative compression, genre/tone shorthand, atmosphere, and viewing-experience framing when these do not change a viewer's material understanding.

Revise when the copy introduces or materially narrows a concrete claim about identity, relationship, agency, motive, causality, mechanism, chronology, quantity, setting, event state, object/entity type, competence/trait, procedural stage, threat/stakes, or spoilers. Use `MINOR` for bounded local concretization; use `SEVERE` for a material contradiction, invention, leak, or distortion. Genre never supplies missing plot mechanics; `facts.keywords` never authorizes semantic editorial copy when `allowedSourceMaterial.keywords` is empty.
